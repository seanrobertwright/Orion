---
phase: 01-foundation-core-tracking
plan: 06
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/app/api/questionnaire/route.ts
  - src/app/api/jobs/[id]/feedback/route.ts
  - src/app/(protected)/questionnaire/page.tsx
  - src/components/questionnaire/QuestionnaireForm.tsx
  - src/components/questionnaire/QuestionnaireBanner.tsx
  - src/components/jobs/JobFeedbackButtons.tsx
  - src/app/(protected)/dashboard/page.tsx (update)
autonomous: true

must_haves:
  truths:
    - "Dashboard shows persistent, dismissible banner prompting questionnaire completion"
    - "Banner reappears periodically if questionnaire not complete"
    - "User can complete questionnaire about skills, preferences, and goals"
    - "User can mark jobs as 'interested' or 'pass' with optional reason"
    - "Questionnaire does NOT block access to any features"
  artifacts:
    - path: "src/app/api/questionnaire/route.ts"
      provides: "CRUD for user questionnaire data"
      exports: ["GET", "PUT"]
    - path: "src/components/questionnaire/QuestionnaireBanner.tsx"
      provides: "Persistent dismissible banner for incomplete questionnaire"
      contains: "dismiss"
    - path: "src/components/jobs/JobFeedbackButtons.tsx"
      provides: "Interested/Pass buttons for job feedback"
      contains: "interested"
    - path: "src/app/api/jobs/[id]/feedback/route.ts"
      provides: "Endpoint to record job interest/pass feedback"
      exports: ["POST"]
  key_links:
    - from: "src/components/questionnaire/QuestionnaireBanner.tsx"
      to: "src/app/api/questionnaire/route.ts"
      via: "GET to check completion status"
      pattern: "fetch.*questionnaire"
    - from: "src/components/jobs/JobFeedbackButtons.tsx"
      to: "src/app/api/jobs/[id]/feedback/route.ts"
      via: "POST to record feedback"
      pattern: "fetch.*feedback"
---

<objective>
Implement non-blocking onboarding questionnaire and job feedback system for preference learning.

Purpose: Collect user preferences for future AI matching while allowing immediate app usage, and track job interest patterns (LEARN-01, LEARN-02, CONTEXT.md non-blocking requirement).

Output: Questionnaire with persistent banner prompts, job interested/pass feedback buttons.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-tracking/01-foundation-core-tracking-CONTEXT.md
@.planning/phases/01-foundation-core-tracking/01-02-SUMMARY.md
@.planning/phases/01-foundation-core-tracking/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Questionnaire API and form</name>
  <files>
    src/app/api/questionnaire/route.ts
    src/app/(protected)/questionnaire/page.tsx
    src/components/questionnaire/QuestionnaireForm.tsx
  </files>
  <action>
    Create src/app/api/questionnaire/route.ts:
    - GET: Return user's questionnaire data (or empty object if not started)
      - Include completionPercentage (0-100)
      - Include isComplete boolean
    - PUT: Update questionnaire fields (partial updates allowed)
      - Set completedAt when all required fields filled
      - Update user.questionnaireCompleted to true when complete

    Questionnaire fields (stored in userQuestionnaire table):
    - technicalSkills: string[] (multi-select or tags)
    - preferredRoles: string[] (Software Engineer, DevOps, Data Scientist, etc.)
    - preferredIndustries: string[] (Tech, Finance, Healthcare, etc.)
    - yearsExperience: number
    - careerGoals: string (textarea)
    - Optional: Remote preference, salary range (already in profile)

    Create src/components/questionnaire/QuestionnaireForm.tsx:
    - Multi-step or single-page form (single page recommended for simplicity)
    - Sections:
      1. Technical Skills (tag input or checkboxes - JavaScript, Python, React, etc.)
      2. Preferred Roles (checkboxes)
      3. Preferred Industries (checkboxes)
      4. Years of Experience (number input or dropdown ranges)
      5. Career Goals (textarea)
    - Progress indicator showing completion percentage
    - "Save Progress" button (saves partial)
    - "Complete" button (only enabled when all required fields filled)
    - Fields are NOT required to navigate away - user can leave anytime

    Create src/app/(protected)/questionnaire/page.tsx:
    - Page title: "Your Preferences"
    - Explanation: "Help us find jobs that match your skills and interests"
    - QuestionnaireForm component
    - "Skip for now" link back to dashboard
    - Success message when complete
  </action>
  <verify>
    GET /api/questionnaire returns current data and completion percentage
    PUT /api/questionnaire with partial data - saves, updates percentage
    PUT /api/questionnaire with all fields - sets completedAt
    User.questionnaireCompleted becomes true when complete
    UI: Fill some fields, leave, come back - data persisted
  </verify>
  <done>
    Questionnaire CRUD and form working (LEARN-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create non-blocking questionnaire banner</name>
  <files>
    src/components/questionnaire/QuestionnaireBanner.tsx
    src/app/(protected)/dashboard/page.tsx (update)
    src/lib/hooks/useQuestionnaireBanner.ts
  </files>
  <action>
    Create src/lib/hooks/useQuestionnaireBanner.ts:
    - Custom hook for banner visibility logic
    - Check localStorage for dismissal timestamp
    - **Per CONTEXT.md - periodically remind:**
      - If dismissed, hide for 24 hours
      - After 24 hours, show again
      - Never show if questionnaire complete
    - Return: { shouldShow, dismiss, completionPercentage }

    Create src/components/questionnaire/QuestionnaireBanner.tsx:
    - **Persistent but dismissible per CONTEXT.md**
    - Styling: Noticeable but not intrusive (blue/info color, not red/error)
    - Content: "Complete your preferences questionnaire to get better job matches"
    - Progress bar showing completion percentage
    - "Complete Now" button -> /questionnaire
    - "Dismiss" button (X icon) -> hides banner, stores timestamp
    - Position: Top of dashboard, below navbar

    Update src/app/(protected)/dashboard/page.tsx:
    - Import QuestionnaireBanner
    - Render banner above MetricsBar
    - Banner handles its own visibility logic (non-blocking)
    - **Dashboard fully usable whether banner shown or not**
  </action>
  <verify>
    New user: Banner appears on dashboard
    Click Dismiss: Banner hides
    Refresh page: Banner still hidden (localStorage)
    Wait 24 hours (or adjust time for testing): Banner reappears
    Complete questionnaire: Banner never shows again
    Dashboard fully functional with or without banner
  </verify>
  <done>
    Non-blocking questionnaire banner with periodic reminders (CONTEXT.md requirement)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create job feedback system (interested/pass)</name>
  <files>
    src/app/api/jobs/[id]/feedback/route.ts
    src/components/jobs/JobFeedbackButtons.tsx
    src/components/jobs/JobCard.tsx (update)
    src/app/(protected)/jobs/[id]/page.tsx (update)
  </files>
  <action>
    Create src/app/api/jobs/[id]/feedback/route.ts:
    - POST: Record job feedback
      - Body: { interested: boolean, reason?: string }
      - Upsert to jobFeedback table (one feedback per user per job)
    - GET: Get current feedback for this job (for UI state)

    Create src/components/jobs/JobFeedbackButtons.tsx:
    - Two buttons: "Interested" (thumbs up/heart), "Pass" (thumbs down/X)
    - Props: jobId, currentFeedback (from API)
    - On "Interested" click:
      - POST { interested: true }
      - Button becomes selected/filled
    - On "Pass" click:
      - Show optional modal: "Why are you passing on this job?" (textarea, optional)
      - POST { interested: false, reason: userInput }
      - Button becomes selected
    - Toggle: clicking selected button removes feedback (deletes record)
    - Visual states: neutral, interested (green), pass (red/gray)

    Update src/components/jobs/JobCard.tsx:
    - Add JobFeedbackButtons to each card
    - Position: Bottom right or as icon buttons
    - Show current feedback state

    Update src/app/(protected)/jobs/[id]/page.tsx:
    - Add JobFeedbackButtons prominently
    - Section showing feedback if already provided
    - Allow editing reason if passed
  </action>
  <verify>
    On job card: See Interested and Pass buttons
    Click Interested: Button shows selected, feedback saved
    Click Pass: Optional reason modal, feedback saved
    Refresh: Feedback state persisted
    Click selected button again: Toggles off (removes feedback)
    Check jobFeedback table: Records created correctly
  </verify>
  <done>
    Job feedback system for preference learning (LEARN-02)
  </done>
</task>

</tasks>

<verification>
1. **Questionnaire page:** Visit /questionnaire, fill fields, save progress
2. **Completion tracking:** Partial completion shows percentage; full completion marks as done
3. **Non-blocking:** Can use dashboard, add jobs, track applications WITHOUT completing questionnaire
4. **Banner persistence:** Banner appears until dismissed; reappears after 24 hours; gone when complete
5. **Job feedback:** Mark job as Interested - saves to database
6. **Pass with reason:** Mark job as Pass, optionally add reason - saves to database
7. **Feedback on cards:** Job cards show feedback buttons; state persists
</verification>

<success_criteria>
- [ ] Questionnaire collects: technical skills, preferred roles, industries, experience, career goals (LEARN-01)
- [ ] Questionnaire is NON-BLOCKING - all features usable without it (CONTEXT.md requirement)
- [ ] Banner is persistent but dismissible (CONTEXT.md requirement)
- [ ] Banner reappears periodically (24hr) until questionnaire complete (CONTEXT.md requirement)
- [ ] User can mark jobs as "interested" (LEARN-02)
- [ ] User can mark jobs as "pass" with optional reason (LEARN-02)
- [ ] Feedback persists and displays on job cards
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-tracking/01-06-SUMMARY.md`
</output>
