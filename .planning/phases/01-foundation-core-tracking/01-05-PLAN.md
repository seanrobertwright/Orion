---
phase: 01-foundation-core-tracking
plan: 05
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/app/api/interviews/route.ts
  - src/app/api/interviews/[id]/route.ts
  - src/app/api/cover-letters/route.ts
  - src/app/api/cover-letters/[id]/route.ts
  - src/app/(protected)/jobs/[id]/interviews/page.tsx
  - src/app/(protected)/cover-letters/page.tsx
  - src/components/interviews/InterviewForm.tsx
  - src/components/interviews/InterviewCard.tsx
  - src/components/cover-letters/CoverLetterForm.tsx
  - src/components/cover-letters/CoverLetterSelect.tsx
autonomous: true

must_haves:
  truths:
    - "User can schedule an interview with date, time, and type"
    - "User can add notes and questions asked after an interview"
    - "User can track interview outcome (passed, advanced, rejected)"
    - "User can create and store cover letters"
    - "User can link a cover letter to an application"
  artifacts:
    - path: "src/app/api/interviews/route.ts"
      provides: "CRUD endpoints for interviews"
      exports: ["GET", "POST"]
    - path: "src/app/api/cover-letters/route.ts"
      provides: "CRUD endpoints for cover letters"
      exports: ["GET", "POST"]
    - path: "src/components/interviews/InterviewForm.tsx"
      provides: "Form for scheduling and editing interviews"
      contains: "scheduledAt"
    - path: "src/components/cover-letters/CoverLetterSelect.tsx"
      provides: "Dropdown to select/link cover letter to application"
      contains: "coverLetterId"
  key_links:
    - from: "src/components/cover-letters/CoverLetterSelect.tsx"
      to: "src/app/api/applications/[id]/route.ts"
      via: "PUT to update coverLetterId"
      pattern: "fetch.*applications.*PUT"
    - from: "src/components/interviews/InterviewForm.tsx"
      to: "src/app/api/interviews/route.ts"
      via: "POST/PUT to create/update interviews"
      pattern: "fetch.*interviews"
---

<objective>
Implement interview scheduling, notes storage, and cover letter management with application linking.

Purpose: Enable users to track the full interview process and manage cover letters linked to specific applications (APP-03, APP-04, APP-05, CV-01, INT-01).

Output: Interview CRUD with outcome tracking, cover letter storage, and application linking.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-tracking/01-02-SUMMARY.md
@.planning/phases/01-foundation-core-tracking/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Interviews API and UI components</name>
  <files>
    src/app/api/interviews/route.ts
    src/app/api/interviews/[id]/route.ts
    src/components/interviews/InterviewForm.tsx
    src/components/interviews/InterviewCard.tsx
    src/app/(protected)/jobs/[id]/interviews/page.tsx
  </files>
  <action>
    Create src/app/api/interviews/route.ts:
    - GET: List interviews for user, optionally filtered by applicationId
      - Include job/application details
      - Sort by scheduledAt (upcoming first)
    - POST: Create new interview
      - Required: applicationId, scheduledAt
      - Optional: type (phone, video, onsite, technical), notes
      - Validate applicationId belongs to user

    Create src/app/api/interviews/[id]/route.ts:
    - GET: Fetch single interview with application/job details
    - PUT: Update interview (scheduledAt, type, notes, questionsAsked, companyFeedback, outcome)
    - DELETE: Delete interview
    - Outcome enum: pending, passed, advanced, rejected

    Create src/components/interviews/InterviewForm.tsx:
    - Fields:
      - Date and Time picker (scheduledAt) - required
      - Type dropdown: Phone Screen, Video Call, On-site, Technical Assessment
      - Notes textarea (pre-interview prep notes)
      - Questions Asked textarea (fill after interview)
      - Company Feedback textarea (fill after interview)
      - Outcome dropdown: Pending, Passed, Advanced to Next Round, Rejected
    - Create mode: minimal fields (date, type)
    - Edit mode: all fields available
    - Save button with loading state

    Create src/components/interviews/InterviewCard.tsx:
    - Display: Date/time, type, outcome badge
    - Expandable to show notes, questions, feedback
    - Edit and delete buttons
    - Color coding by outcome (green=passed/advanced, red=rejected, gray=pending)

    Create src/app/(protected)/jobs/[id]/interviews/page.tsx:
    - Or integrate into existing job detail page as a section
    - List all interviews for this job's application
    - "Schedule Interview" button opens InterviewForm
    - Timeline view of interview history
  </action>
  <verify>
    POST /api/interviews with applicationId and date - creates interview
    GET /api/interviews?applicationId=X - returns interviews for that application
    PUT /api/interviews/[id] with outcome - updates interview
    UI: Schedule interview, see it in list, edit outcome, delete
  </verify>
  <done>
    Interview scheduling, notes, and outcome tracking working (APP-04, APP-05, INT-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Cover Letters API and management UI</name>
  <files>
    src/app/api/cover-letters/route.ts
    src/app/api/cover-letters/[id]/route.ts
    src/app/(protected)/cover-letters/page.tsx
    src/components/cover-letters/CoverLetterForm.tsx
    src/components/cover-letters/CoverLetterCard.tsx
  </files>
  <action>
    Create src/app/api/cover-letters/route.ts:
    - GET: List all cover letters for user
      - Return id, name, preview (first 200 chars), createdAt
    - POST: Create new cover letter
      - Required: name, content
      - Validate name is unique per user

    Create src/app/api/cover-letters/[id]/route.ts:
    - GET: Fetch full cover letter content
    - PUT: Update cover letter name and/or content
    - DELETE: Delete cover letter
      - Remove link from any applications using it (set coverLetterId to null)

    Create src/components/cover-letters/CoverLetterForm.tsx:
    - Fields:
      - Name (text input, e.g., "Software Engineer - General", "Startup Cover Letter")
      - Content (large textarea, supports markdown or plain text)
    - Character/word count display
    - Save button with loading state

    Create src/components/cover-letters/CoverLetterCard.tsx:
    - Display: Name, preview snippet, created date
    - Click to expand/edit
    - Delete button with confirmation
    - Badge showing how many applications use this letter

    Create src/app/(protected)/cover-letters/page.tsx:
    - Page title: "Cover Letters"
    - List of all cover letters
    - "New Cover Letter" button
    - Click card to edit
    - Empty state: "No cover letters yet. Create one to use with your applications."
  </action>
  <verify>
    POST /api/cover-letters with name and content - creates letter
    GET /api/cover-letters - returns list with previews
    PUT /api/cover-letters/[id] - updates letter
    DELETE /api/cover-letters/[id] - removes letter, unlinks from applications
    UI: Create, view, edit, delete cover letters
  </verify>
  <done>
    Cover letter CRUD working (CV-01)
  </done>
</task>

<task type="auto">
  <name>Task 3: Link cover letters to applications</name>
  <files>
    src/components/cover-letters/CoverLetterSelect.tsx
    src/app/(protected)/jobs/[id]/page.tsx (update)
  </files>
  <action>
    Create src/components/cover-letters/CoverLetterSelect.tsx:
    - Dropdown component to select a cover letter
    - Props: applicationId, currentCoverLetterId, onChange
    - Fetch cover letters from /api/cover-letters
    - Options: "None", plus all user's cover letters
    - On select: PUT /api/applications/[id] with { coverLetterId }
    - "Create New" option that links to /cover-letters with return URL

    Update src/app/(protected)/jobs/[id]/page.tsx:
    - Add "Cover Letter" section
    - CoverLetterSelect dropdown to link/change cover letter
    - If cover letter linked: Show name and preview
    - "View Full" button to expand cover letter content
    - "Edit" link to /cover-letters page

    Update src/app/api/applications/[id]/route.ts (if not already):
    - PUT handler accepts coverLetterId field
    - Validate coverLetterId belongs to user (if provided)
  </action>
  <verify>
    On job detail page: See cover letter dropdown
    Select a cover letter - linked to application
    Change to different cover letter - updates link
    Select "None" - removes link
    Refresh page - link persisted
    View cover letter preview on job page
  </verify>
  <done>
    Cover letters linkable to applications (APP-03)
  </done>
</task>

</tasks>

<verification>
1. **Interview scheduling:** Create interview for a job application with date/time
2. **Interview types:** Can select phone, video, onsite, technical
3. **Interview notes:** Add notes before interview, questions/feedback after
4. **Interview outcome:** Set outcome to passed/advanced/rejected
5. **Cover letter CRUD:** Create, view, edit, delete cover letters
6. **Cover letter linking:** Select cover letter on job detail page, saves to application
7. **Link persistence:** Refresh job page - cover letter still linked
</verification>

<success_criteria>
- [ ] User can schedule interviews with date, time, type (APP-04)
- [ ] User can store interview notes and questions asked (APP-05, INT-01)
- [ ] User can track interview outcome (pass, advance, rejected) (APP-05)
- [ ] User can create and store cover letters (CV-01)
- [ ] User can link cover letter to application (APP-03)
- [ ] Cover letter preview visible on job detail page
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-tracking/01-05-SUMMARY.md`
</output>
