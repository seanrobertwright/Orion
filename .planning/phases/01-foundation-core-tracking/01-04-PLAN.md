---
phase: 01-foundation-core-tracking
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/app/(protected)/dashboard/page.tsx
  - src/components/dashboard/KanbanBoard.tsx
  - src/components/dashboard/KanbanColumn.tsx
  - src/components/dashboard/KanbanCard.tsx
  - src/components/dashboard/ListView.tsx
  - src/components/dashboard/MetricsBar.tsx
  - src/components/dashboard/ViewToggle.tsx
  - src/components/dashboard/SearchFilter.tsx
  - src/app/api/dashboard/stats/route.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard defaults to Kanban board view showing jobs as cards in status columns"
    - "User can switch to List view as secondary option"
    - "Dashboard shows three key metrics: Jobs in Queue, Interviewed, Stale Jobs"
    - "Stale jobs (applied >14 days) are visually flagged with red/warning styling"
    - "User can drag cards between columns to change status (status history logged)"
    - "User can search jobs by title or company name"
    - "User can filter jobs by status and location"
  artifacts:
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Main dashboard page with view toggle and metrics"
      contains: "KanbanBoard"
    - path: "src/components/dashboard/KanbanBoard.tsx"
      provides: "Kanban board with columns for each status"
      contains: "saved.*applied.*interviewing.*offered.*rejected"
    - path: "src/components/dashboard/MetricsBar.tsx"
      provides: "Top metrics display"
      contains: "stale"
    - path: "src/components/dashboard/SearchFilter.tsx"
      provides: "Search and filter controls"
      contains: "search"
    - path: "src/app/api/dashboard/stats/route.ts"
      provides: "Statistics endpoint for dashboard metrics"
      exports: ["GET"]
  key_links:
    - from: "src/components/dashboard/KanbanBoard.tsx"
      to: "src/app/api/applications/[id]/status/route.ts"
      via: "PATCH on drag-drop status change"
      pattern: "fetch.*status"
    - from: "src/components/dashboard/MetricsBar.tsx"
      to: "src/app/api/dashboard/stats/route.ts"
      via: "fetch stats on mount"
      pattern: "fetch.*stats"
    - from: "src/components/dashboard/SearchFilter.tsx"
      to: "src/app/api/jobs/route.ts"
      via: "query params for search/filter"
      pattern: "search=.*status="
---

<objective>
Create the job search dashboard with Kanban board (default), List view, key metrics display, and search/filter functionality.

Purpose: Give users a visual overview of their job search pipeline with actionable metrics and ability to find specific jobs (APP-06, JOBS-02, CONTEXT.md dashboard requirements).

Output: Interactive dashboard with draggable Kanban cards, list view toggle, real-time metrics, and search/filter controls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-tracking/01-foundation-core-tracking-CONTEXT.md
@.planning/phases/01-foundation-core-tracking/01-02-SUMMARY.md
@.planning/phases/01-foundation-core-tracking/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard statistics API</name>
  <files>
    src/app/api/dashboard/stats/route.ts
  </files>
  <action>
    Create src/app/api/dashboard/stats/route.ts:
    - GET: Return dashboard statistics for authenticated user
    - Calculate and return:
      ```typescript
      {
        jobsInQueue: number,      // Count of jobs with status 'saved'
        totalActive: number,      // Count of jobs NOT in 'rejected' or 'offered'
        interviewed: number,      // Count of jobs that reached 'interviewing' status (check statusHistory)
        staleJobs: number,        // Count of jobs in 'applied' status > 14 days
        totalApplied: number,     // Count of jobs with status 'applied' or beyond
        totalResponses: number,   // Count of jobs that moved past 'applied' (interviewing, offered, rejected)
        responseRate: number,     // (totalResponses / totalApplied) * 100, or 0 if no applications
        interviewRate: number,    // (interviewed / totalApplied) * 100, or 0 if no applications
      }
      ```
    - Use efficient SQL queries with Drizzle (avoid N+1)
    - For "interviewed" count: Query statusHistory where toStatus = 'interviewing'
  </action>
  <verify>
    GET /api/dashboard/stats returns all metrics
    Add jobs in different statuses - counts update correctly
    Response rate and interview rate calculate correctly
  </verify>
  <done>
    Dashboard statistics API returning all required metrics
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Kanban board and search/filter components</name>
  <files>
    src/components/dashboard/KanbanBoard.tsx
    src/components/dashboard/KanbanColumn.tsx
    src/components/dashboard/KanbanCard.tsx
    src/components/dashboard/SearchFilter.tsx
    src/lib/hooks/useDragAndDrop.ts
  </files>
  <action>
    Create src/lib/hooks/useDragAndDrop.ts:
    - Custom hook for drag-and-drop functionality
    - Use native HTML5 drag-and-drop API (no external library needed for basic functionality)
    - Handle dragStart, dragOver, dragEnd events
    - Return handlers and state for drop zones

    Create src/components/dashboard/SearchFilter.tsx:
    - **Search input:** Text field for searching by job title or company name (JOBS-02)
    - **Status filter:** Multi-select or dropdown to filter by status(es)
    - **Location filter:** Text input or dropdown for location filtering
    - **Clear filters:** Button to reset all filters
    - Debounced search (300ms) to avoid excessive API calls
    - Props: onFilterChange(filters), currentFilters
    - URL state sync: Persist filters to URL params for shareability

    Create src/components/dashboard/KanbanCard.tsx:
    - Props: job (with application), isStale, onStatusChange
    - Display: Company name (bold), Job title, Location (if set)
    - **Stale styling per CONTEXT.md:** If isStale=true:
      - Red border or red background tint
      - "STALE" badge visible
    - Draggable with drag handle
    - Click to navigate to /jobs/[id]
    - Compact design to fit multiple cards per column

    Create src/components/dashboard/KanbanColumn.tsx:
    - Props: status, title, jobs[], onDrop
    - Column header with status name and count
    - Drop zone styling (highlight on drag over)
    - Scrollable if many cards
    - Empty state message if no jobs

    Create src/components/dashboard/KanbanBoard.tsx:
    - Fetch jobs from /api/jobs with filter params on mount and filter change
    - 5 columns: Saved, Applied, Interviewing, Offered, Rejected
    - On card drop to different column:
      1. Optimistic UI update
      2. PATCH /api/applications/[id]/status
      3. Rollback on error
    - Responsive: horizontal scroll on mobile
    - Pass filter state down, refetch when filters change
  </action>
  <verify>
    Kanban board displays with 5 columns
    Cards appear in correct columns by status
    Drag card from Saved to Applied - status changes
    Drag card from Applied to Offered - allowed (flexible flow)
    Stale cards (applied >14 days) show red styling
    Search "Google" - only jobs with Google in title or company shown
    Filter by "Applied" status - only applied jobs shown
  </verify>
  <done>
    Kanban board with drag-and-drop and search/filter functionality
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard page with metrics, view toggle, and list view</name>
  <files>
    src/app/(protected)/dashboard/page.tsx
    src/components/dashboard/MetricsBar.tsx
    src/components/dashboard/ListView.tsx
    src/components/dashboard/ViewToggle.tsx
  </files>
  <action>
    Create src/components/dashboard/MetricsBar.tsx:
    - Fetch stats from /api/dashboard/stats
    - Display 3 key metrics per CONTEXT.md:
      1. **Jobs in Queue:** "{jobsInQueue} / {totalActive}" with label "Jobs in Queue"
      2. **Interviewed:** "{interviewed}" with label "Interviewed"
      3. **Stale Jobs:** "{staleJobs}" with label "Stale" - **RED color if > 0**
    - Additional stats row (smaller): Response Rate, Interview Rate
    - Auto-refresh every 60 seconds or on focus

    Create src/components/dashboard/ViewToggle.tsx:
    - Two buttons/tabs: "Board" (Kanban icon), "List" (list icon)
    - Persist preference to localStorage
    - **Default to "Board" per CONTEXT.md**

    Create src/components/dashboard/ListView.tsx:
    - Table/list view of all jobs
    - Columns: Company, Title, Status, Applied Date, Location, Actions
    - **Sortable columns** (click header to sort)
    - Status dropdown for quick changes
    - Stale indicator in status column
    - Higher data density than Kanban
    - Respects same search/filter state as Kanban

    Create src/app/(protected)/dashboard/page.tsx:
    - Page title: "Job Search Dashboard"
    - MetricsBar at top (always visible)
    - SearchFilter below metrics
    - ViewToggle below search
    - **Default view: KanbanBoard (per CONTEXT.md)**
    - Conditional render: KanbanBoard or ListView based on toggle
    - Both views share the same filter state
    - "Add Job" button linking to /jobs/new
    - Loading skeleton while fetching
  </action>
  <verify>
    Visit /dashboard - see metrics bar at top
    Default view is Kanban board (not list)
    Click List toggle - switches to list view
    Refresh page - view preference persisted
    Stale job count shows in red when > 0
    Response rate and interview rate display correctly
    Search works in both Kanban and List views
    Filter by status works in both views
  </verify>
  <done>
    Dashboard with Kanban default, list secondary, metrics, and search/filter per CONTEXT.md
  </done>
</task>

</tasks>

<verification>
1. **Default view:** Visit /dashboard -> Kanban board shown (not list)
2. **Metrics display:** Top bar shows Jobs in Queue (X/Y), Interviewed (Z), Stale Jobs (N)
3. **Stale highlighting:** Stale jobs count is red; stale cards have red border
4. **View toggle:** Click List -> See table view; Click Board -> Back to Kanban
5. **View persistence:** Toggle to List, refresh, still List
6. **Drag-and-drop:** Drag card from Saved to Applied -> Status changes, history logged
7. **Flexible status:** Can drag directly from Saved to Offered (no linear restriction)
8. **Stats accuracy:** Add jobs, change statuses -> Metrics update correctly
9. **Search:** Type company name -> Only matching jobs shown (JOBS-02)
10. **Filter:** Select status filter -> Only jobs with that status shown (JOBS-02)
11. **Combined filter:** Search + status filter work together
</verification>

<success_criteria>
- [ ] Dashboard defaults to Kanban board view (CONTEXT.md requirement)
- [ ] List view available as secondary option (CONTEXT.md requirement)
- [ ] Metrics bar shows: Jobs in Queue, Interviewed, Stale Jobs (CONTEXT.md requirement)
- [ ] Stale jobs (applied > 14 days) flagged with red styling (CONTEXT.md requirement)
- [ ] Drag-and-drop changes status and logs to history
- [ ] Dashboard shows application statistics (APP-06)
- [ ] Response rate and interview rate calculated and displayed (APP-06)
- [ ] User can search jobs by title, company (JOBS-02)
- [ ] User can filter jobs by status and location (JOBS-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-tracking/01-04-SUMMARY.md`
</output>
